mixin pagination(entity, filter, total, perPage)
    -const pages = Math.ceil(total / perPage);
    nav.pagination
        ul
            each _, i in Array(pages)
                li: a(href=`/${entity}/${filter}/${i+1}`)= i+1
    p= `${page} of ${pages}`
