p 
    label(for="first_name") First Name
    input(id="first_name" type="text" required value=`${row.first_name}`)
p 
    label(for="last_name") Last Name
    input(id="last_name" type="text" required value=`${row.last_name}`)
p 
    label(for="address") Address
    input(id="address" type="text" required value=`${row.address}`)
p 
    label(for="email") Email 
    input(id="Email" type="text" required value=`${row.email}`)
p 
    label(for="library_id") Library ID
    input(id="library_id" type="text" required value=`${row.library_id}`)
p 
    label(for="zip_code") Zip Code
    input(id="zip_code" type="text" required value=`${row.zip_code}`)
