p 
    label(for="title") Title
    input(id="title" name="title" type="text" required value=`${row.title}`)
p 
    label(for="author") Author            
    input(id="author" name="author" type="text" required value=`${row.author}`)
p 
    label(for="genre") Genre            
    input(id="genre" name="genre" type="text" required value=`${row.genre}`)
p 
    label(for="first_published") First Published 
    input(id="first_published" name="first_published" type="text" required value=`${row.first_published}`)
